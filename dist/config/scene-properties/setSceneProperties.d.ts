import { ScenePropertiesConfig } from "../config.types";
export declare const getScenePropertiesFromConfig: (config?: ScenePropertiesConfig) => {
    viewWidth: string;
    viewHeight: string;
    backgroundColor: string;
    backgroundUrl: string;
    position: string;
};
