import { ScreenType } from "../../compat/window-state/types";
import { SceneConfig } from "../config.types";
export declare const useScreenSizeProperties: (config: SceneConfig | undefined | null, currentScreenType: ScreenType) => SceneConfig | null | undefined;
