import { Bounds3D } from "./three-dimension-space/position/position.types";
export declare const getRandomCoordinates: (numberOfCoodinates: number, boundingBox: Bounds3D) => {
    x: number;
    y: number;
    z: number;
}[];
